import { StyleSheet } from "react-native";

export const SignInStyles = StyleSheet.create({
  container : {
    flex : 1, 
  },
  input: {
    justifyContent : "center",
    height: 40,
    margin: 12,
    borderWidth: 1,
    padding: 10,
  },
});
